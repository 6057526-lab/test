# 🏒 HockeyGearBot - Система управления хоккейной экипировкой

## 📋 Описание проекта

Telegram-бот для управления продажами хоккейной экипировки. Система позволяет вести учет товаров, партий, продаж, остатков, устанавливать цены и получать аналитику.

## 🏗️ Архитектура проекта

### 📁 Структура файлов

```
test/
├── main.py                 # Точка входа приложения
├── config.py              # Конфигурация (токены, настройки)
├── requirements.txt       # Зависимости Python
├── README.txt            # Документация проекта
├── migrate_db.py         # Миграции базы данных
├── check_config.py       # Проверка конфигурации
├── dump_my_project.py    # Экспорт структуры проекта
│
├── data/                 # Слой данных
│   ├── db.py            # Настройка SQLAlchemy и сессий
│   ├── models.py        # ORM модели (Agent, Batch, Product, Sale, etc.)
│   └── db.sqlite3       # SQLite база данных
│
├── services/            # Бизнес-логика (разделена по модулям)
│   ├── core_service.py      # Фасад для всех операций
│   ├── agent_service.py     # Работа с агентами
│   ├── batch_service.py     # Управление партиями товаров
│   ├── sales_service.py     # Продажи и бонусы
│   ├── stock_service.py     # Остатки и поиск товаров
│   ├── price_service.py     # Управление ценами
│   ├── report_service.py    # Отчеты и графики
│   └── gear_service.py      # Подбор хоккейной экипировки
│
├── handlers/            # Обработчики Telegram событий
│   ├── __init__.py      # Регистрация хендлеров и FSM состояния
│   ├── admin_handlers.py # Административные функции
│   ├── sales_handlers.py # Продажи и остатки
│   ├── user_handlers.py  # Общие пользовательские функции
│   └── gear_handlers.py  # Подбор экипировки
│
├── utils/               # Утилиты
│   └── tools.py         # Вспомогательные функции (графики, Excel)
│
├── uploads/             # Загруженные файлы
├── venv/               # Виртуальное окружение Python
└── code_dumps/         # Документация кода
```

## 🔧 Технический стек

### Основные технологии
- **Python 3.11+** - основной язык
- **aiogram 3.x** - Telegram Bot API framework
- **SQLAlchemy 2.x** - ORM для работы с БД
- **SQLite** - база данных
- **pandas** - обработка Excel файлов
- **matplotlib** - генерация графиков

### Ключевые библиотеки
- `openpyxl` - работа с Excel файлами
- `python-dotenv` - загрузка переменных окружения
- `loguru` - логирование (опционально)

## 🎯 Основные функции

### 👥 Управление агентами
- Автоматическое создание агентов при первом использовании
- Система бонусов на основе продаж
- Выплата бонусов администраторами

### 📦 Управление товарами
- **Загрузка партий из Excel** с валидацией данных
- **Автоматический расчет себестоимости** (цена + логистика + курс)
- **Управление остатками** с учетом продаж
- **Поиск товаров** по EAN, названию, модели

### 💰 Управление ценами
- **Установка розничных цен** для отдельных товаров
- **Массовое обновление цен** по фильтрам
- **Процентное повышение** от себестоимости
- **История изменений цен** с логированием
- **Предпросмотр изменений** перед применением

### 🛒 Продажи
- **Оформление продаж** с расчетом маржи
- **Проверка остатков** перед продажей
- **Рекомендуемые цены** (РРЦ, себестоимость, последняя продажа)
- **Возвраты** с возвратом товара на склад
- **Автоматический расчет бонусов**

### 📊 Остатки и поиск
- **Улучшенный интерфейс остатков** с сортировкой и пагинацией
- **Цветовая индикация** уровня остатков (🔴≤2, 🟡3-5, 🟢>5)
- **Фильтрация по категориям, размерам, складам**
- **Экспорт в Excel** текущего набора товаров
- **Поиск с информацией об остатках**

### 🏒 Подбор экипировки
- **Интерактивная анкета** подбора по позиции, уровню, возрасту, бюджету
- **Готовые комплекты** экипировки (вратарь, игрок, профессиональный)
- **Умный поиск** совместимых товаров
- **Рекомендации** по подбору размеров и совместимости

### 📈 Аналитика и отчеты
- **Графики продаж** по периодам (7/30/90 дней)
- **Маржинальность по категориям**
- **Динамика цен** по товарам
- **Отчеты по продажам** с группировкой по агентам
- **Экспорт данных** в Excel

## 🗄️ Модели данных

### Основные сущности

#### Agent (Агент)
- `telegram_id` - ID в Telegram
- `telegram_username` - username в Telegram
- `full_name` - полное имя
- `is_admin` - права администратора

#### Batch (Партия)
- `batch_number` - номер партии (автогенерация)
- `warehouse` - склад
- `created_by_id` - создатель
- `created_at` - дата создания

#### Product (Товар)
- `ean` - штрих-код (уникальный в рамках партии)
- `name`, `model`, `color`, `size`, `age` - характеристики
- `fit` - тип посадки (regular/tapered/wide)
- `weight` - вес в кг
- `quantity` - количество в партии
- `price_eur` - цена в евро
- `exchange_rate`, `coefficient` - курсовые коэффициенты
- `logistics_per_kg` - логистика за кг
- `cost_price` - рассчитанная себестоимость
- `retail_price` - розничная цена
- `batch_id` - связь с партией

#### Sale (Продажа)
- `product_id`, `agent_id` - связи
- `quantity` - количество
- `sale_price` - цена продажи
- `margin` - маржа (общая)
- `margin_percent` - маржа в процентах
- `warehouse` - склад продажи
- `is_returned` - флаг возврата

#### Bonus (Бонус)
- `agent_id`, `sale_id`, `rule_id` - связи
- `amount` - сумма бонуса
- `percent_used` - использованный процент
- `is_paid` - выплачен ли бонус

#### PriceHistory (История цен)
- `product_id`, `changed_by_id` - связи
- `old_price`, `new_price` - старые и новые цены
- `changed_at` - дата изменения

#### StockLog (Движение товара)
- `product_id` - товар
- `operation_type` - тип операции (in/out/return)
- `quantity` - количество
- `warehouse` - склад
- `reference_id` - ссылка на операцию

#### ActionLog (Лог действий)
- `agent_id` - кто выполнил
- `action_type` - тип действия
- `entity_type`, `entity_id` - сущность
- `details` - детали

## 🔄 Бизнес-процессы

### 1. Загрузка товаров
1. Администратор загружает Excel файл с товарами
2. Система валидирует данные и проверяет дубликаты
3. Создается партия с автоматическим номером
4. Рассчитывается себестоимость для каждого товара
5. Товары добавляются в остатки

### 2. Установка цен
1. Администратор выбирает товары по фильтрам
2. Устанавливает новую цену или процент повышения
3. Система показывает предпросмотр изменений
4. После подтверждения применяются изменения
5. Сохраняется история изменений

### 3. Продажа товара
1. Агент ищет товар по EAN или названию
2. Система показывает информацию и рекомендуемые цены
3. Агент вводит цену продажи
4. Система проверяет остатки и создает продажу
5. Рассчитывается маржа и бонус
6. Остатки автоматически уменьшаются

### 4. Просмотр остатков
1. Пользователь заходит в раздел остатков
2. Система показывает компактный список с сортировкой
3. Доступна пагинация и фильтрация
4. Цветовая индикация уровня остатков
5. Возможен экспорт в Excel

## 🎨 Пользовательский интерфейс

### Главное меню
- **🛒 Продажи** - оформление продаж
- **📦 Остатки** - просмотр остатков товаров
- **💰 Бонусы** - управление бонусами (агенты)
- **⚙️ Админ** - административные функции

### Административные функции
- **📤 Загрузить партию** - загрузка товаров из Excel
- **💰 Установить цены** - массовое управление ценами
- **📈 Графики** - аналитика и отчеты
- **📊 Отчеты** - детальные отчеты по продажам

### Улучшенный интерфейс остатков
- **Компактное отображение** - 8 товаров на страницу
- **Цветовая индикация** - 🔴≤2, 🟡3-5, 🟢>5
- **Сортировка** - по имени, остатку, цене
- **Пагинация** - навигация по страницам
- **Экспорт** - выгрузка в Excel

## ⚙️ Конфигурация

### Основные настройки (config.py)
```python
TOKEN = "your_bot_token"
ADMIN_IDS = [123456789]  # ID администраторов
EXCEL_TEMPLATE_COLUMNS = [...]  # Колонки Excel шаблона
DEFAULT_WAREHOUSES = [...]  # Склады по умолчанию
BONUS_RULES = [...]  # Правила бонусов
```

### Переменные окружения (.env)
```env
BOT_TOKEN=your_bot_token
ADMIN_IDS=123456789,987654321
LOG_ACTIONS=true
```

## 🚀 Запуск проекта

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации
- Скопируйте `.env.example` в `.env`
- Укажите токен бота и ID администраторов

### 3. Инициализация базы данных
```bash
python migrate_db.py
```

### 4. Запуск бота
```bash
python main.py
```

## 📊 Производительность

### Оптимизации
- **Индексы БД** на часто используемых полях
- **CTE запросы** для сложных агрегаций
- **Предзагрузка связанных данных** (joinedload)
- **Пагинация** для больших списков
- **Кэширование** фильтров

### Мониторинг
- **Логирование действий** пользователей
- **Отслеживание ошибок** с контекстом
- **Метрики производительности** запросов

## 🔒 Безопасность

### Права доступа
- **Администраторы** - полный доступ ко всем функциям
- **Агенты** - только продажи и просмотр остатков
- **Автоматическое создание** агентов при первом использовании

### Валидация данных
- **Проверка Excel файлов** на соответствие шаблону
- **Валидация EAN** и других полей
- **Проверка остатков** перед продажей
- **Логирование** всех критических операций

## 🛠️ Разработка

### Структура сервисов
Проект использует **модульную архитектуру** с разделением ответственности:

- **AgentService** - управление агентами
- **BatchService** - загрузка и управление партиями
- **SalesService** - продажи и бонусы
- **StockService** - остатки и поиск
- **PriceService** - управление ценами
- **ReportService** - отчеты и аналитика

### Добавление новых функций
1. Создайте метод в соответствующем сервисе
2. Добавьте делегирование в `CoreService`
3. Создайте обработчик в соответствующем модуле handlers
4. Обновите документацию

### Тестирование
```bash
# Проверка синтаксиса
python -m py_compile main.py

# Запуск с логированием
python main.py
```

## 📈 Планы развития

### Краткосрочные улучшения
- [ ] **Push-уведомления** для низких остатков
- [ ] **Интеграция с Google Sheets** для экспорта/импорта
- [ ] **Поиск по фото** (заглушка с потенциалом ML)
- [ ] **PDF чеки** с брендингом

### Долгосрочные планы
- [ ] **Redis** для FSM и кэширования
- [ ] **Docker** контейнеризация
- [ ] **Alembic** для миграций БД
- [ ] **Unit тесты** для сервисов
- [ ] **CI/CD** пайплайн

## 🤝 Поддержка

### Логирование
- Все действия пользователей логируются в `ActionLog`
- Ошибки записываются с полным контекстом
- Доступно логирование в файл или консоль

### Отладка
- Проверка конфигурации: `python check_config.py`
- Экспорт структуры: `python dump_my_project.py`
- Логирование SQL запросов в режиме разработки

## 📝 Лицензия

Проект разработан для внутреннего использования. Все права защищены.

---

**Версия:** 2.0  
**Последнее обновление:** Август 2025  
**Автор:** HockeyGearBot Team

