README (draft)
Описание
Телеграм‑бот помогает вести учёт и продажи хоккейной экипировки: приёмку товаров из Excel, установку цен, оформление продаж и возвратов, расчёт бонусов и формирование отчётов.

Стек
Python, aiogram v3 – обработка Telegram‑сообщений

SQLAlchemy + SQLite – база данных

pandas / openpyxl – импорт и экспорт Excel

python‑dotenv – загрузка настроек

Структура проекта
.
├── main.py               # точка входа
├── config.py             # конфигурация и пути
├── check_config.py       # проверка .env и токена
├── migrate_db.py         # миграция БД (добавляет ограничение EAN+партия)
├── data/
│   ├── db.py             # инициализация engine и сессий
│   ├── models.py         # модели Agent, Batch, Product, Sale и др.
│   └── db.sqlite3        # файл БД
├── handlers/handlers.py  # все хендлеры aiogram
├── services/core_service.py # бизнес‑логика
└── utils/tools.py        # вспомогательные функции и отчёты
Настройка окружения
Установите Python 3.11+ и создайте виртуальное окружение.

Установите зависимости:

pip install -r requirements.txt
Создайте файл .env в корне проекта:

BOT_TOKEN=<токен бота>
ADMIN_IDS=123456789,987654321   # через запятую
При отсутствии токена скрипт завершится с предупреждением.

(Опционально) запустите проверку конфигурации:

python check_config.py
Скрипт подскажет наличие .env, токена и ID админов.

Запуск бота
python main.py
main() инициализирует базу, создаёт бота и регистрирует хендлеры.

Основные возможности
1. Старт и выбор роли
Команда /start регистрирует пользователя в БД и показывает клавиатуру администратора или продавца в зависимости от ID.

2. Приёмка партии по Excel
Администратор отправляет команду «📅 Приемка партии». Бот предлагает скачать шаблон Excel и затем обработает загруженный файл, спросив склад, на который приходит партия.
Загрузка формирует запись Batch и товары Product через CoreService.create_batch_from_excel().

3. История продаж
Команда «📈 История продаж» показывает последние 30 дней с маржой и ценой каждой позиции.
История берётся через CoreService.get_agent_sales_history().

4. Возвраты
Администратор вызывает «↩️ Возврат», вводит ID продажи и причину. Бот отмечает продажу как возвращённую и возвращает товар на склад.

5. Остатки и поиск
CoreService.get_stock_optimized() формирует остатки по складам с учётом продаж и возвратов, используя SQL‑CTE для оптимальной выборки.

6. Формирование отчётов
В utils/tools.py реализовано форматирование карточки товара, отчёты по продажам и бонусам, генерация Excel с остатками и чеков продаж.

Модели данных
data/models.py определяет структуру БД:

Agent, Batch, Product — основные сущности склада и товаров.

Sale, BonusRule, Bonus, PriceHistory, StockLog, ActionLog — продажи, бонусы и история изменений.

Дополнительные сценарии
Проверка и миграция БД для добавления уникального ограничения на EAN внутри партии:
python migrate_db.py.

Добавление новых хендлеров: зарегистрируйте их в handlers/handlers.py и вызовите register_handlers() при запуске.

